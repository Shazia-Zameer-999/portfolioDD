/** @type {import('next').NextConfig} */
const nextConfig = {
  // This allows your phone (and any other device) to access the dev server
  allowedDevOrigins: ["http://192.168.1.41:3000"],
};

export default nextConfig;
